#include<iostream>
#define min 10
int b = 1;
void combination(const int* a,int size) {
	if (size != 7) {
		int get = size / 2;
	}
	for (int i = 0; i < size; i++) {

	}
}

int main() {
	int a[] = { 1,2,3,4,5,6,7 };
	int size = sizeof(a) / sizeof(a[0]);
	//combination(a, size);
	std::cout << size;
}
